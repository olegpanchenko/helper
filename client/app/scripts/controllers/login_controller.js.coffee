angular.module("helperClientApp").controller "LoginCtrl", ["$scope", "$location", \
                            ($scope, $location) ->
  # $state, $stateParams, , Account, Skill
  # , "$state", "$stateParams", "Account", "Skill"
]